import React from "react";

const Result = ({ result }) => {
  return (
    <div>
      <section className="result">
        <p>{result.Title}</p>
      </section>
    </div>
  );
};

export default Result;
